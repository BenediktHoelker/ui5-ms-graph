<mvc:View controllerName="com.iot.ui5-ms-graph.controller.Authentication" 
	xmlns:mvc="sap.ui.core.mvc" 
	xmlns:unified="sap.ui.unified" 
	xmlns:core="sap.ui.core" displayBlock="true" 
	xmlns="sap.m">
	<Shell id="shell">
		<App id="app">
			<pages>
				<Page id="page" title="Arbeitszeit">
					<headerContent>
						<Button icon="sap-icon://person-placeholder" text="{= ${session>/userData} === undefined ? 'Login' : 'Logout ' + ${session>/userData/givenName}}" press="onSwitchSession"/>
					</headerContent>
					<content>
						<SinglePlanningCalendar id="PC1" startDateChange="onStartDateChange" headerDateSelect="onHeaderDateSelect" startDate="{session>/startDate}" appointmentSelect="handleAppointmentSelect" appointments="{path : '/'}">
							<appointments>
								<CalendarAppointment startDate="{start}" endDate="{end}" icon="{pic}" title="{subject}" text="{location/displayName}" tentative="{tentative}">
								</CalendarAppointment>
							</appointments>
						</SinglePlanningCalendar>
					</content>
				</Page>
			</pages>
		</App>
	</Shell>
</mvc:View>